{
  "name": "@mission/mobile",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "echo 'Expo build configured externally'",
    "typecheck": "tsc --noEmit",
    "test": "echo 'No tests configured yet'",
    "lint": "echo 'No lint configured yet'"
  },
  "devDependencies": {
    "typescript": "^5.6.2"
  }
}
